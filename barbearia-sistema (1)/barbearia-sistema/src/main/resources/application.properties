# ---------- Aplicacao ----------
spring.application.name=sistema-barbearia
server.port=8080

# ---------- Banco H2 em memoria (trocar por MySQL/Postgres em producao) ----------
spring.datasource.url=jdbc:h2:mem:barbearia
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# ---------- Thymeleaf ----------
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# ---------- Exemplo de configuracao MySQL (descomente e ajuste se quiser trocar o banco) ----------
# spring.datasource.url=jdbc:mysql://localhost:3306/barbearia?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=sua_senha
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
